<nav class="navbar navbar-fixed-top navbar-inverse" style="line-height: 50px; padding: 0px 25px 0px 0px">
  <div class="container-fluid">
    <% if logged_in? %>
      <ul class="nav navbar-nav navbar-left">
        <li style="font-weight: bold;">
          <%= link_to "PÁGINA INICIAL", root_url, :style => "color: white;" %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li style="font-weight: bold;">
          <%= link_to "SAIR", logout_path, method: "delete", :style => "color: white;" %>
        </li>
      </ul>
    <% else %>
      <ul class="nav navbar-nav navbar-left">
        <li style="font-weight: bold;"><a href"#" style="color: white;">MOTORISTAS</a></li>
      </ul>
    <% end %>
  </div>
</nav>
